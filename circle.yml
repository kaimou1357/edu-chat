machine:
  timezone:
    America/Los_Angeles
  java:
    version: oraclejdk8
dependencies:
  pre:
    - sudo apt-get update
test:
  override:
    - (export TERM="dumb"; if [ -e ./gradlew ]; then ./gradlew test || ./gradlew test ; else gradle test || gradle test;fi)